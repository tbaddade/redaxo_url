{
    "require": {
        "dergel/kit-urlparser": "^2.3.0"
    },
    "scripts": {
        "post-install-cmd": [
            "rm -rf vendor/composer vendor/dergel/kit-urlparser/examples && rm -f vendor/autoload.php"
        ],
        "post-update-cmd": [
            "rm -rf vendor/composer vendor/dergel/kit-urlparser/examples && rm -f vendor/autoload.php"
        ]
    },
    "replace": {
        "psr/http-message": "*"
    }
}
